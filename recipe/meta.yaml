{% set name = "rb-fsevent" %}
{% set version = "0.10.4" %}

package:
  name: rb-{{ name|lower }}
  version: {{ version }}

source:
  url: https://rubygems.org/downloads/{{ name }}-{{ version }}.gem
  sha256: 0741522cac72db1fc15250453b557a57f896a60ede7ba64f73e281688fd42bcd

build:
  noarch: generic
  number: 0
  script:
    - gem install -N -l -V --norc --ignore-dependencies {{ name }}-{{ version }}.gem
    - gem unpack {{ name }}-{{ version }}.gem
  skip: {{ win }}

requirements:
  host:
    - ruby
  run:
    - ruby

test:
  commands:
    - ruby -r {{ name }} -e 'exit 0'

about:
  home: http://rubygems.org/gems/{{ name }}
  license: MIT
  license_family: MIT
  license_file: {{ name }}-{{ version }}/LICENSE.txt
  summary: FSEvents API with Signals catching (without RubyCocoa)

  dev_url: https://github.com/thibaudgg/rb-fsevent

extra:
  recipe-maintainers:
    - sodre
